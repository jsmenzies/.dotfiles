# Pi extensions

Put project-specific or personal global Pi extensions (`*.ts`) in this directory.

This directory is symlinked to `~/.config/pi/agent/extensions` by the install script so extensions are shared across machines.
